---
'solid-floating-ui': patch
---

- The README now links to a `docs/` directory covering positioning, interactions, components, transitions, utilities, recipes and migration from `@floating-ui/react`.
